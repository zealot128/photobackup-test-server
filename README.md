## Photobackup Test-server

Simple Ruby Sinatra test server for mimicking the PhotoBackup protocol for testing purposes.


## Installation

* Install Ruby >=2

```
bundle install
rackup
```

Then, on port 9292 the server will be running.

